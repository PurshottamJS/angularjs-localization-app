;
(function(window, angular, undefined) {
    'use strict';
    angular
        .module('app.localize')
        .value('localeSupported', [
            'en-US',
            'fr-FR',
        ])
}(window, angular, undefined));
